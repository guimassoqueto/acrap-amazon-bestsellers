## Scrap Amazon Bestsellers

```shell
make env
```

```shell
poetry shell && poetry install
```

```shell
pre-commit install && pre-commit autoupdate && pre-commit install --hook-type commit-msg
```

```shell
make a
```